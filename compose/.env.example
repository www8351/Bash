# Copy to .env and adjust. Compose reads .env automatically.
# No secrets here — ports and image tags only.

# Host ports
NGINX_PORT=8080
HTTPD_PORT=8081
PORTAINER_PORT=9000

# Image tags (pin in production instead of latest)
NGINX_TAG=latest
HTTPD_TAG=latest
PORTAINER_TAG=latest
